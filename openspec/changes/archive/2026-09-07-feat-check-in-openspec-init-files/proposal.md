## Why

This pull request adds the repository files generated by `openspec init`, but it currently has no active OpenSpec change documenting that work. The repository requires every pull request to carry an OpenSpec entry, so the bootstrap check-in needs a concise, durable planning record before review can continue.

## What Changes

- Record the purpose and scope of the OpenSpec initialization check-in.
- Document the generated workflow skills, target marker, and initialized archive/spec directory markers already included in this pull request.
- Add a design record and verifiable task checklist for the repository-tooling check-in.
- Deliberately keep the change limited to OpenSpec and agent-workflow metadata; no application source, tests, or runtime behavior changes are included.

## Capabilities

### New Capabilities

None. This is a repository tooling/bootstrap change and does not introduce product behavior requirements.

### Modified Capabilities

None.

## Impact

The change affects repository OpenSpec metadata and agent workflow discoverability under `openspec/` and `.agents/skills/`. It does not change public application APIs, runtime behavior, dependencies, or persisted user data.
